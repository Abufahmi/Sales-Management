@using Library.Contracts

@page "/Dashboard"
@layout DashLayout
@attribute [Authorize(Roles = nameof(AppServices.Admin))]
@inject IAccountService repo

<PageTitle>@Localizer[nameof(Translate.SiteName)] | @Localizer[nameof(Translate.Dashboard)]</PageTitle>
<DashCrumbComponent />

@code {
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }
}
